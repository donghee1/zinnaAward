<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zinnaworks.mapper.MemberMapper">
	<insert id="insertMemberInfo"
		parameterType="com.zinnaworks.vo.Member">
		INSERT INTO USERS
		(USER_ID, PASSWD, USER_NM, GRP_CD ,ENTRY_DT, PHONENUM, GRADE_CD)
		VALUES(#{userId},#{passWd},#{userNm},#{grpCd},
		#{entryDt},#{phoneNum}, #{gradeCd});
	</insert>

	<select id="selectLoginInfo"
		resultType="com.zinnaworks.vo.Member">
		SELECT * FROM USERS
		WHERE USER_ID = #{userId} AND PASSWD
		= #{password}
	</select>

	<select id="selectCheckById"
		resultType="com.zinnaworks.vo.Member">
		SELECT * FROM USERS
		WHERE USER_ID = #{userId};
	</select>

	<select id="loadUserByUsername"
		resultType="com.zinnaworks.vo.Member">
		SELECT * FROM USERS
		WHERE USER_ID = #{userId}
	</select>

	<update id="updateMemberPwd"
		parameterType="com.zinnaworks.vo.Member">
		UPDATE USERS SET pwd = #{pwd}
		WHERE USER_ID = #{userId}
	</update>

	<insert id="mergeInsertAuthInfo"
		parameterType="com.zinnaworks.vo.Mail">
		INSERT INTO USER_AUTH(USER_ID, AUTH_KEY, CRE_DT, EXP_DT)
		VALUES(#{userId},#{authKey},#{creDt},#{expDt})
	</insert>

 <select id="selectAuthInfo" parameterType="String" resultType="com.zinnaworks.vo.MailAuth">
		SELECT *
		FROM USER_AUTH
		WHERE USER_ID=#{email} AND EXP_DT = (SELECT MAX(EXP_DT) FROM USER_AUTH)  
	</select>  
	
	<!-- <update id="mergeUpdateAuthInfo"
		parameterType="com.zinnaworks.vo.Mail">
		MERGE INTO USER_AUTH
		USING USER_AUTH
		ON (USER_ID = #{address})
		WHEN MATCHED THEN
		UPDATE SET AUTH_KEY=#{AUTH_KEY}, CRE_DT=#{CRE_DT}, EXP_DT=#{EXP_DT}
		WHERE USER_ID=#{address}
	</update> -->
	
	<!-- <update id="mergeInsertAuthInfo"
		parameterType="com.zinnaworks.vo.Mail">
		MERGE INTO USER_AUTH
		USING USER_AUTH
		ON (USER_ID=#{address})
		WHEN NOT MATCHED THEN
		INSERT INTO (USER_ID, AUTH_KEY, CRE_DT,EXP_DT)
		VALUES (#{address},#{AUTH_KEY},#{CRE_DT},#{EXP_DT})
	</update> -->

</mapper>